package mapping

import tree.FunctionNode

import scala.collection.mutable

object DerivativeMapping {
  val MAPPING: mutable.HashMap[String, FunctionNode] =
    mutable.HashMap[String, FunctionNode] (
    )
}
